export const QORTINO_OPTIONAL_IDLE_FACTS = [
  'QORTINO stays local-first here. He reacts to your Hub state, not a remote profile tracker.',
  'Notifications only mute desktop popups. In-Hub activity and unread states still keep flowing.',
  'Wallet backup exports locally. This flow is about your device and your keys, not cloud sync.',
  'Most Hub tools here run inside the same closed app environment. QORTINO is companion logic, not telemetry.',
] as const;
